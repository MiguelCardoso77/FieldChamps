import {StyleSheet} from "react-native";

export const Styles = StyleSheet.create({
    pageContainer: {
        flex: 1,
        backgroundColor: '#1A1717',
        marginTop: 50,
        marginBottom: 50,
        padding: 20,
    },
});